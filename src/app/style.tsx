import {StyleSheet, Dimensions} from 'react-native';
import { ColorEnum } from './data/enum';
const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: ColorEnum.Black,
      color:'white',
      alignItems: 'center',
    },
  });
  

  export default styles;